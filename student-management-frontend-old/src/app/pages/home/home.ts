import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-home',
  imports: [RouterLink],
  templateUrl: './home.html', // Correct the template URL here
  styleUrl: './home.css'
})
export class Home { // The class is named Home

}